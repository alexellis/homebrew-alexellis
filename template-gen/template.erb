class <%= $project.capitalize %> < Formula
    desc "<%= @desc %>"
    url "<%= @url %>"
    sha256 "<%= @sha256 %>"
    version "<%= @version %>"
    bottle :unneeded
    
    def install
        bin.install "<%= $project %>-darwin" => "<%= $project %>"
    end
end